datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["business", "channels", "rbac", "parameters"]
}

generator client {
  provider        = "prisma-client-js"
  moduleFormat    = "cjs"
}